apiVersion: v1
kind: Service
metadata:
    name: node-example-service
spec:
    selector:
        app: node-example
    type: LoadBalancer
    ports:
        - protocol: TCP
          port: 4000
          targetPort: 4000
